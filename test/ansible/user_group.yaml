---
- name: Cluster Deployer playbook
  hosts: vag
  tasks:
    - include_role:
        name: user_group
      tags:
       - user_creation
#- hosts: vag
#  ignore_errors: true
#  roles:
#    - { role: user_group, tags: user_creation_with_uid }
      # - { role: user_group, user_name: aaa, group_name: fuck , shell_type: /bin/bash, tags: [ 'user_creation'] }

#   - { role: demo, jino: "user" }
    # TAGS:
    # 1.user_creation
    # 2.user_creation_with_shell
    # 3.user_delete
    # 4.user_creation_with_uid
    # 5.user_creation_with_single_group
    # 6.user_creation_with_multiple_group
    # 7.group_creation

    #   variables:
    # 1.user_name:
    # 2.shell_type
    # 3.group_name
    # 4.uid_num
    # 5.group_name
