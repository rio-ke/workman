---
- name: play for ntp
  hosts: vag
  tasks:
  roles:
    - { role: ntp, ntp_servers: [ 0.asia.pool.ntp.org, 1.asia.pool.ntp.org, 2.asia.pool.ntp.org, 3.asia.pool.ntp.org ]}
    - { role: firewall, firewall_service: [ntp, http]}
